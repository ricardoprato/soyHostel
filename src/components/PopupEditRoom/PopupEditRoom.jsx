import React from 'react';
import { useNavigate } from 'react-router-dom';

const PopupEditRoom = () => {
  const navigate = useNavigate();

  return <div>PopupEditRoom</div>;
};

export default PopupEditRoom;
